# Database (opzionale: se non impostato, usa SQLite locale turni.db)
# DATABASE_URL=postgresql://user:password@host:5432/colazione

# JWT Secret (OBBLIGATORIO in produzione quando DATABASE_URL è impostato)
# JWT_SECRET=cambia-con-un-segreto-sicuro-di-almeno-32-caratteri

# Admin iniziale (opzionale: se non impostato, genera password random in console)
# ADMIN_DEFAULT_PASSWORD=la-tua-password-admin
# ADMIN_USERNAME=admin
